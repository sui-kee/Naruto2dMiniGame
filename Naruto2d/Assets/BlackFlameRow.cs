using UnityEngine;

public class BlackFlameRow : MonoBehaviour
{
    [SerializeField] private GameObject BlackFlame1;
    [SerializeField] private GameObject BlackFlame2;
    [SerializeField] private GameObject BlackFlame3;
    [SerializeField] private GameObject BlackFlame4;
    [SerializeField] private GameObject BlackFlame5;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
